/// <reference types="react" />
/**
 * A wrapping ActionButton.
 * Should only be rendered if a valid wrap exists.
 */
export default function WrapButton({ disabled }: {
    disabled: boolean;
}): JSX.Element;
